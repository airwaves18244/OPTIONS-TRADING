interface:
  display_name: "OPTIONS TRADING"
  short_description: "Repo-specific patterns and workflows for OPTIONS-TRADING"
  default_prompt: "Use the OPTIONS-TRADING repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true